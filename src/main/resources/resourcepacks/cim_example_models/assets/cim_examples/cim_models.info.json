{
    /*
     * > В 'entries' идёт перечисление как:
     * |- Список поддерживаемых предметов,
     * |- Компоненты (предикаты), своего рода фильтр,
     * \- Модель, которая будет подменена;
     * если условия из 'items' и 'predicate' будут выполнены.
     * > Проще говоря: если текущий предмет - это предмет из 'items',
     * и у этого предмета есть нужные предикаты - то определяем модель из 'model'.
     * > Дальше контроль над отображением берёт 'properties.json',
     * который лежит внутри указанной модели:
     * cim:magic_sword -> cim:cim_models/magic_sword/properties.json,
     * или же как: assets/cim/cim_models/magic_sword/properties.json
    */
    "entries": [
        {
            /*
             * > ID предмета (или же ItemID), который будет подвержен изменениям.
             * > Можно указать сколько угодно, для того и сделан в виде списка.
             * > Если не был указан 'namespace' (до ':'), то по умолчанию майн
             * подставляет свой `namespace`, который `minecraft`.
            */
            "items": [ "stick", "namespace:item_id" ],

            /*
             * > Компоненты (предикаты), которые выступают в свою очередь как фильтр.
             * > Если у предмета есть один из перечисленных компонентов и
             * значение этого компонента равно (или эе другое условие сравнения),
             * то видоизменяются именно эти предметы.
            */
            "components": {
                // Указывается как 'ключ: значение' //
                "custom_model_data": 12345,

                // Так же можно иметь вложенные компоненты //
                "potion_contents": { "potion": "water" }
            },

            /*
             * > Режим фильтрации компонентов у предметов:
             * |- `all`  - все перечисленные компоненты должны быть у предмета с указанными значениями
             * |- `any`   - любой из компонентов должен быть у предмета с указанными значениями
             * \- `only` - у предмета должны быть только данные компоненты с указанными значениями. Ничего лишнего
             * > Игнорируются компоненты с динамическими значениями (такие как урон предмета, натяжение лука) т.к. они уже контролируются в контроллере анимаций
            */
            "mode": "any",

            /* > Теперь здесь указывается просто путь к модели
             * > !!! НЕ К ФАЙЛУ 'properties.json' !!!
             * > Пример: значение -> конвертация -> реальный путь
             * cim:cim_matrix_cube -> cim:cim_models/matrix_cube -> assets/cim/cim_models/matrix_cube
             * mc_item_custom -> minecraft:cim_models/mc_item_custom -> assets/cim/cim_models/mc_item_custom
            */
            "model": "cim:matrix_cube",

            /*
             * > Убедитесь что путь корректный, и 'реальный путь' соответствует ожидаемому.
             * > Иначе за место своей модели - вы увидите 'ERROR'-модель :|.
            */
        }
    ]
}